let bool = true;


document.querySelector(".button_green").addEventListener("click",)
document.querySelector(".button_toggle").addEventListener("click",)

fonction rainbow() {
    let tab = {"red", "green", "blue", "purple", "pink", "orange"}
}

fonction toggleButton() {
    if (bool) {
        changeCouleur?ckassBale : "boite";
        bool = false;
       }else {
            changeCouleur.className = "boite black";
            bool = true;
        }
}